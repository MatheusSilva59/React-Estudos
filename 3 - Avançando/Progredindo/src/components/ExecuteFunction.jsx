const ExecuteFunction = ({myFunction}) => {
  return (
    <div>
        <button onClick={myFunction}>Click here to execute the function, then check the logs.</button>
    </div>
  )
}

export default ExecuteFunction